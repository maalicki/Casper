{% for label, flashes in app.session.flashbag.all %}
    {% for flash in flashes %}
        <div class="alert alert-{{ label }}" style="margin-top: 20px;">
            {{ flash }}
        </div>
    {% endfor %}
{% endfor %}